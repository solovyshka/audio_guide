YANDEX_API_KEY=
# Optional, only if the API asks for a folder:
# YANDEX_FOLDER_ID=

# City pipeline (OpenAI Responses + web_search).
# On FIREBAT copied onto the host by deploy/secrets/apply-local.sh.
# Traffic to api.openai.com goes through OVH HTTP proxy (tinyproxy :8888).
OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4.1
# OPENAI_QA_MODEL=gpt-4.1
# OPENAI_TIMEOUT=600
# QA_RETRIES=4
# HTTPS_PROXY=http://127.0.0.1:8888
